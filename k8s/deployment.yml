apiVersion: apps/v1
kind: Deployment  
metadata:
  name: k8s-argocd-deployment
spec:
  replicas: 7
  selector:
    matchLabels:
      app: k8s-argocd
  template:
    metadata:
      labels:
        app: k8s-argocd
    spec:
      containers:
      - name: k8s-argocd-container
        image: anoshdock/k8s-cicd-argocd:v1.1.1
        imagePullPolicy: Always
        ports:
        - containerPort: 5000

        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5

        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10
